# PLACEHOLDER: needs to be moved to work laptop file
# TODO just print
# serial
# issuer / CN
# validity
# subject
# v3 extensions
alias x509='openssl x509 -noout -text -in'
alias ssh_rsakey='ssh-keygen -t rsa -b 4096'
alias ssh_edkey='ssh-keygen -t ed25519'

# k8s
# needs kubectl, kubectx, kubens installed
alias kcx='kubectl ctx'
alias kcl='kubectl'
alias kcn='kubectl ns'


